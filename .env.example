# 環境変数テンプレート
# 利用方法: cp .env.example .env  → 必要な値を埋める
# 本ファイルは Git にコミットしてよいが、.env 本体はコミット禁止

# Next.js ランタイム
NODE_ENV=production
PORT=3000
HOSTNAME=0.0.0.0

# Next.js テレメトリ送信を無効化（推奨）
NEXT_TELEMETRY_DISABLED=1

# ---- 将来用のサンプル枠（現状アプリは未使用） ----
# NEXT_PUBLIC_API_BASE_URL=
# DATABASE_URL=
